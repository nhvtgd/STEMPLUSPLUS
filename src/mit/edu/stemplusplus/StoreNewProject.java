package mit.edu.stemplusplus;

import android.app.Activity;
import android.os.Bundle;

public class StoreNewProject extends Activity {

  // either pushes the project to the parse server or store it locally
    
    @Override
    public void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
 
        //ProjectParcel obj = b.getParcelable("mit.edu.stemplusplus.ProjectParcel");
    }
}
